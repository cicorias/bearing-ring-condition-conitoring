# Azure infrastructure

This directory is reserved for future Azure Developer CLI (`azd`) infrastructure and deployment
configuration.

No Azure resources are required for the current local model demo. Future infrastructure should
deploy the model API and Microsoft Agent Framework application as separate components with explicit
contracts, identities, telemetry, and lifecycle controls.
